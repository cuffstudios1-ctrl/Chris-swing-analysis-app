import Link from 'next/link'; import { demoSwings } from '@/lib/demo';
export function Header(){return <header className="sticky top-0 z-10 border-b border-white/10 bg-[#08110d]/90 backdrop-blur"><nav className="mx-auto flex max-w-6xl items-center justify-between p-4"><Link href="/" className="text-xl font-black">Fairway Frame</Link><div className="flex gap-2 text-sm"><Link className="btn btn2" href="/new">New swing</Link><Link className="btn btn2" href="/history">History</Link></div></nav></header>}
export function Disclaimer(){return <p className="rounded-2xl border border-amber-300/30 bg-amber-300/10 p-4 text-sm text-amber-50">AI-generated practice guidance only. It may be incorrect, is not a substitute for an in-person qualified coach, and you should stop any drill that causes pain.</p>}
export function SwingCard({s=demoSwings[0]}:{s?:any}){return <div className="card p-5"><p className="text-sm text-white/60">{new Date(s.createdAt).toLocaleDateString('en-GB')} • {s.club} • {s.cameraAngle==='down-the-line'?'Down the line':'Face on'}</p><h3 className="mt-2 text-xl font-bold">{s.primaryIssue}</h3><p className="mt-1 text-white/70">Status: {s.processingStatus}</p><Link className="btn mt-4" href={`/report/${s.id}`}>Open report</Link></div>}

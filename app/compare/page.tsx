import { demoSwings } from '@/lib/demo';
export default function Compare(){const [a,b]=demoSwings;return <div className="space-y-5"><h1 className="text-3xl font-black">Swing comparison</h1><div className="grid gap-5 lg:grid-cols-2">{[a,b].map((s,i)=><div className="card p-5" key={s.id}><h2 className="text-xl font-bold">{i?'Current':'Previous'} swing</h2><img src={s.videoUrl} alt="Video placeholder" className="mt-3 rounded-2xl"/><p>{new Date(s.createdAt).toLocaleDateString('en-GB')} • {s.club}</p><p><b>Primary issue:</b> {s.primaryIssue}</p><p><b>Strongest feature:</b> {s.strongestFeature}</p><div className="mt-3 grid grid-cols-3 gap-2">{s.frames.slice(0,3).map(f=><img key={f.id} src={f.imageUrl} alt={f.label} className="rounded-lg"/>)}</div><p className="mt-3"><b>Recommendation:</b> {s.analysis.nextSession.practiceFocus}</p></div>)}</div><section className="card p-5"><h2 className="text-2xl font-bold">Cautious AI comparison</h2><ul className="list-disc pl-5 text-white/80"><li><b>Appears improved:</b> finish balance looks a little more stable in the later sample.</li><li><b>Unchanged:</b> delivery-space priority remains the main item.</li><li><b>May have worsened:</b> no clear worsening visible from the sample frames.</li><li><b>Next single practice priority:</b> film half-speed swings while preserving hip depth and hand space.</li></ul><p className="mt-3 text-white/60">Do not claim improvement if camera angle, timing or quality makes comparison unreliable.</p></section></div>}
